
# FVM Version Cache
.fvm/

# Keep this app's resolved package graph reproducible.
!pubspec.lock

# Firebase native config files (contain secrets, regenerated locally)
macos/Runner/GoogleService-Info.plist

# Superdeck build output (thumbnails, generated assets)
.superdeck/*
!.superdeck/.gitkeep
